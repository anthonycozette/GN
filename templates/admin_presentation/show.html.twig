{% extends 'base.html.twig' %}

{% block title %}Presentation{% endblock %}

{% block body %}
    <h1>Presentation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ presentation.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ presentation.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ presentation.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_presentation_index') }}">back to list</a>

    <a href="{{ path('admin_presentation_edit', {'id': presentation.id}) }}">edit</a>

    {{ include('admin_presentation/_delete_form.html.twig') }}
{% endblock %}
